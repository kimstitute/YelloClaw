# TypeScript Interfaces

`src/types.ts` is the canonical implementation source.

## What this document is for

- A short pointer for type-oriented docs
- A map to the runtime-owned type source

## Canonical source

- Implementation types: `src/types.ts`
- Storage schema: `docs/storage-schema.md`
- Kakao contracts: `docs/kakao-contracts.md`

## Notes

Keep detailed interface edits in `src/types.ts` and update the related schema docs only when the contract changes.
